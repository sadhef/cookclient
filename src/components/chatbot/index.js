import ChatBot from './ChatBot';
import ChatToggle from './ChatToggle';

// Export both components for easier imports
export { ChatBot, ChatToggle };

// Default export the main component
export default ChatBot;